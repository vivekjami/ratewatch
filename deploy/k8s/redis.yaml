apiVersion: apps/v1
kind: Deployment
metadata:
  name: ratewatch-redis
  namespace: ratewatch
  labels:
    app: ratewatch-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ratewatch-redis
  template:
    metadata:
      labels:
        app: ratewatch-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        command:
        - redis-server
        - --appendonly
        - "yes"
        - --maxmemory
        - "256mb"
        - --maxmemory-policy
        - "allkeys-lru"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: redis-data
          mountPath: /data
      volumes:
      - name: redis-data
        persistentVolumeClaim:
          claimName: redis-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ratewatch-redis
  namespace: ratewatch
spec:
  selector:
    app: ratewatch-redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  namespace: ratewatch
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi